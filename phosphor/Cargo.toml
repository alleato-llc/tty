[package]
name = "phosphor"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
cathode = { path = "../cathode" }
iced = { workspace = true }
parking_lot = { workspace = true }
# Open a URL (Open Link) in the OS default handler.
open = "5"
